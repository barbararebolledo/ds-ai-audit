{
  "$schema": "https://json-schema.org/draft-07/schema",
  "$id": "editorial-schema.json",
  "title": "Audit Editorial Content v1.1",
  "$comment": "Prose overrides only. This schema carries human-written content that overrides generated text in the audit and remediation files. It does not carry remediation structure. Joined to audit and remediation files by audit_id. The front-end merges this with the data JSON at render time, preferring editorial content when present. All sections are optional; the front-end falls back to data JSON content when editorial content is absent.",

  "type": "object",
  "required": ["meta", "scope_statement", "organisational_implications"],
  "additionalProperties": false,

  "properties": {

    "meta": {
      "type": "object",
      "required": ["schema_version", "audit_ref"],
      "additionalProperties": false,
      "properties": {
        "schema_version": {
          "type": "string",
          "const": "1.1",
          "$comment": "Editorial schema version. Independent from audit schema version."
        },
        "audit_ref": {
          "type": "string",
          "$comment": "Must match the audit_id in the companion data JSON. Links the two files."
        },
        "last_edited_by": {
          "type": "string",
          "$comment": "Optional. Name of the last person who edited this file."
        },
        "last_edited_at": {
          "type": "string",
          "format": "date-time",
          "$comment": "Optional. ISO 8601 timestamp of the last edit."
        }
      }
    },

    "scope_statement": {
      "type": "string",
      "$comment": "Required. Multi-paragraph prose rendered after the executive summary and before the cluster scores. States what the audit measures and does not measure, framing the score boundary as the instrument's edges rather than its gaps. Contains {overall_score} and {phase_readiness} placeholders interpolated from the companion audit JSON summary at front-end render time. The 'edges, not gaps' framing and the four-item scope list (organisational capacity, designer judgement, accessibility-in-the-trenches, brand-as-lived-practice) are non-negotiable. Do not soften 'It does not claim to measure' with 'fully' or 'comprehensively'."
    },

    "organisational_implications": {
      "type": "object",
      "$comment": "Required. Reads artefact patterns as shadows of organisational capacity. Never asserts a diagnosis. Phrasing pattern: suggests / pattern / validate-with-you. 'Often suggests' is the load-bearing modal phrase -- not 'indicates', not 'shows that'. 'We'd like to validate this with you in Phase 2' closes every pattern without variation.",
      "required": ["opening", "patterns", "closing"],
      "additionalProperties": false,
      "properties": {
        "opening": {
          "type": "string",
          "$comment": "Invariant. Do not edit. Rendered before the patterns array."
        },
        "patterns": {
          "type": "array",
          "$comment": "Per-audit. Select 3-5 patterns based on the score profile. Each pattern is a three-part block: artefact pattern, suggested implication, validation invitation.",
          "items": {
            "type": "object",
            "required": ["pattern_name", "cluster_or_dimension_reference", "score_value", "supporting_observation", "artefact_pattern_described", "organisational_implication"],
            "additionalProperties": false,
            "properties": {
              "pattern_name": {
                "type": "string",
                "$comment": "Short label naming the artefact pattern."
              },
              "cluster_or_dimension_reference": {
                "type": "string",
                "$comment": "Which scoring unit anchors the pattern."
              },
              "score_value": {
                "type": "string",
                "$comment": "Numeric score or score contrast the pattern is read from."
              },
              "supporting_observation": {
                "type": "string",
                "$comment": "The second data point that triangulates the pattern."
              },
              "artefact_pattern_described": {
                "type": "string",
                "$comment": "The artefact-level pattern in plain language."
              },
              "organisational_implication": {
                "type": "string",
                "$comment": "The suggested organisational reading in tentative phrasing ('often suggests...'). Do not assert. Do not diagnose."
              }
            }
          }
        },
        "closing": {
          "type": "string",
          "$comment": "Invariant. Do not edit. Rendered after the patterns array."
        }
      }
    },

    "tiers": {
      "$comment": "Optional. Keyed by tier number ('1', '2', '3'). Defines the label and value framing for each remediation tier. Tier-level effort is computed by the dashboard from item totals, not stored here.",
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["label", "value_framing"],
        "additionalProperties": false,
        "properties": {
          "label": {
            "type": "string",
            "$comment": "Client-facing tier name, e.g. 'Agent Readability'."
          },
          "value_framing": {
            "type": "string",
            "$comment": "Why this tier matters operationally. Rendered on the remediation page above the tier's item list."
          }
        }
      }
    },

    "report": {
      "$comment": "Optional. Report-level prose for client-facing output. Has no counterpart in the data JSON.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "title": {
          "type": "string",
          "$comment": "Client-facing report title, e.g. 'AI-Readiness Audit: Material UI'."
        },
        "executive_summary": {
          "type": "string",
          "$comment": "Overall narrative for stakeholders. Two to four sentences summarising the audit state and the single most important action."
        },
        "methodology_note": {
          "type": "string",
          "$comment": "Brief explanation of the audit approach for readers unfamiliar with the methodology."
        }
      }
    },

    "clusters": {
      "$comment": "Optional. Keyed by cluster key (e.g. '1_token_and_variable_system'). Overrides or supplements cluster-level prose in the data JSON.",
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "narrative": {
            "type": "string",
            "$comment": "Editorial override of the cluster_summary field in the data JSON. Used in dashboards and executive summaries."
          },
          "value_framing": {
            "type": "string",
            "$comment": "What this cluster's current state means operationally. Used on the Impact screen. Example: 'Token architecture gaps mean every theme change requires manual updates across all components using flat values.'"
          }
        }
      }
    },

    "dimensions": {
      "$comment": "Optional. Keyed by dimension ID (e.g. '3.1_functional_intent_coverage'). Overrides dimension-level prose in the data JSON.",
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "narrative": {
            "type": "string",
            "$comment": "Editorial override of the dimension narrative field in the data JSON. Used in dimension detail views."
          }
        }
      }
    },

    "findings": {
      "$comment": "Optional. Keyed by finding ID (e.g. 'CDC-001'). Overrides finding-level prose in the data JSON. The front-end prefers these values when present.",
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "summary": {
            "type": "string",
            "$comment": "Edited one-line overview. Overrides the summary field in the data JSON."
          },
          "description": {
            "type": "string",
            "$comment": "Edited full description. Overrides the description field in the data JSON."
          },
          "recommendation": {
            "type": "string",
            "$comment": "Edited recommendation text. Overrides the recommendation field in the data JSON."
          }
        }
      }
    },

    "remediation": {
      "$comment": "Optional. Keyed by remediation item ID (e.g. 'REM-001'). Overrides remediation-level prose in the data JSON.",
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "action": {
            "type": "string",
            "$comment": "Edited action text. Overrides the action field in the data JSON."
          },
          "value_framing": {
            "type": "string",
            "$comment": "Edited operational consequence text. Overrides the value_framing field in the data JSON."
          }
        }
      }
    }
  }
}
